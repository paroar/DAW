Escribe un bloque anónimo que use el procedimiento del ejercicio 23.

declare

 v_nom a.emp.nom%type;
 v_apes a.emp.apes%type;
 v_flag boolean;

begin

 proc23('0',v_nom,v_apes,v_flag);

 if v_flag
  then
   dbms_output.put_line(v_nom||' '||v_apes);
  else
   dbms_output.put_line('Empleado no existe');
 end if;

end;